---
description: Control claude-voice — stop, mute [30m|2h|1d], unmute, status, stats [24h|7d]
---

Run this via Bash: `node "${CLAUDE_PLUGIN_ROOT}/dist/cli.js" $ARGUMENTS`
(no arguments → run `status`). Relay its output back verbatim (in a code block if multi-line), nothing else.
